name: posture_reminder_app
description: Aplicación de recordatorios de postura corporal para personas mayores
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  intl: any
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0
  
  # State Management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  
  # Local Storage
  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # Firebase
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.2
  firebase_messaging: ^16.0.2
  
  # Notifications
  flutter_local_notifications: ^19.4.2
  timezone: ^0.10.1
  permission_handler: ^12.0.1
  
  # Utils
  uuid: ^4.2.2
  dartz: ^0.10.1
  get_it: ^8.2.0
  injectable: ^2.3.2
  flutter_localization: ^0.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.7
  injectable_generator: ^2.4.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/icon.png"

flutter_native_splash:
  color: "#ffffff"
  image: assets/icons/splash_icon.png
  android: true
  ios: true

  android_12:
    image: assets/icons/splash_icon.png
    
  web: false